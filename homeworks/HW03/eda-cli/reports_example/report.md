# EDA-отчёт

Исходный файл: `example.csv`

Строк: **36**, столбцов: **14**

## Качество данных (эвристики)

- Оценка качества: **0.49**
- Макс. доля пропусков по колонке: **5.56%**
- Слишком мало строк: **True**
- Слишком много колонок: **False**
- Слишком много пропусков: **False**

### Новые эвристики качества

1. **Константные колонки**: False

2. **Высокая кардинальность категориальных признаков** (>50 уникальных значений): False

3. **Подозрительные дубликаты ID**: True
   - Возможный ID с дубликатами (35 уникальных из 36) (дубликатов: 2.8%)
## Колонки

См. файл `summary.csv`.

## Пропуски

См. файлы `missing.csv` и `missing_matrix.png`.

## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

Топ-5 значений по категориальным признакам см. в папке `top_categories/`.

## Гистограммы числовых колонок

Построено гистограмм для первых 6 числовых колонок.
См. файлы `hist_*.png`.
